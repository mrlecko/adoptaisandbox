apiVersion: v1
kind: Service
metadata:
  name: {{ include "csv-analyst-chat.fullname" . }}
  labels:
    {{- include "csv-analyst-chat.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "csv-analyst-chat.selectorLabels" . | nindent 4 }}
