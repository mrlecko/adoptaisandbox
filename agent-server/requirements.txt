# CSV Analyst Chat - Agent Server Dependencies

# Core framework
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.10,<3.0
pydantic-settings>=2.5,<3.0

# LangChain and AI
langchain>=0.3,<0.4
langchain-anthropic>=0.2,<0.4
langchain-openai>=0.2,<0.4
langgraph>=0.2,<2.0

# Execution backends
docker>=7,<8  # For DockerExecutor
kubernetes>=29,<34  # For K8sJobExecutor

# Database
aiosqlite>=0.20,<1.0  # For async capsule storage

# Utilities
python-multipart>=0.0.9,<0.1  # For file uploads (future)
python-dotenv>=1.0,<2.0  # For .env support
mlflow[genai]>=2.16,<4.0  # Observability, tracing, and GenAI integrations
prometheus-client>=0.20,<1.0  # Lightweight production metrics endpoint

# Testing
pytest>=8,<9
pytest-asyncio>=0.24,<1.0
pytest-cov>=5,<7
httpx>=0.27,<1.0  # For testing FastAPI

# Development
black>=24.8,<26.0
ruff>=0.6,<1.0
mypy>=1.11,<2.0
